@model List<MaromFit.Models.Plano>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Planos</h2>
@{ var planCol = Model;}
    <ul>

        @foreach (var planos in planCol)
        {
            <li> @planos.Name / @planos.Value R$</li>
        }

    </ul>



